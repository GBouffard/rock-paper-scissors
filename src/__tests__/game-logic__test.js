import gameLogic from '../services/game-logic';

describe('game logic', () => {
  xit('chooses a random hand for the CPU when playing a 1 player game', () => {
  });

  xit('knows when the result of a game is a draw', () => {
  });

  xit('knows when the player won the game', () => {
  });

  xit('knows when the player lost the game', () => {
  });
});
